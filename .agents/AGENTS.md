# Project Rules

## Auto Commit & Push
Setelah selesai mengerjakan task, selalu lakukan:
1. `git add .`
2. `git commit -m "<deskripsi perubahan>"`
3. `git push origin master`

Agar user bisa langsung deploy di Easypanel tanpa repot.
